import { Meta, Story } from "@storybook/addon-docs";

import FileList from "./FileList";

<Meta title="FileBrowser/FileList" component={FileList} />

# Master browser component

This is the final exported browser component

export const Template = (args) => <FileList {...args} />;

<Story name="FileList" args={{ data: { hello: "yes", dirA: { yes: "no" } } }}>
  {Template.bind({})}
</Story>
